server:
  port: 7054
spring:
  application:
    name: configserver
    
    
  cloud:
   config:
     server:
       git:
         uri: https://github.com/NikStack20/Hotel-Review-System-Configs
         clone-on-start: true